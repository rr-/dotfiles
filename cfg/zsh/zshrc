# autocomplete
autoload -Uz compinit                   # initialize autocompletion engine
autoload -Uz edit-command-line          # initialize additional modules
compinit -i                             # initialize autocompletion engine (2)
zstyle ':completion:*' matcher-list \
    'm:{a-zA-Z}={A-Za-z}' \
    '+l:|=* r:|=*'                      # recursive
setopt NO_NOMATCH                       # if extended glob fails, run cmd as-is
setopt NO_CASE_GLOB                     # ignore case in glob searches
setopt nullglob                         # nonexisting wildcards expand to ''
source ~/.zprofile
eval "$(zoxide init zsh)"


# color theme
# The theme belongs to the terminal we are attached to, not to this machine: an
# ssh client can run a light theme while the local session is dark. Resolve it
# once per shell, most specific source first, and export it so that tmux,
# neovim and the rest don't each have to guess again.
#
# Nothing below assumes the terminal honors OSC 4 palette remaps - the colors
# that matter are spelled out in rgb, because indices 16-255 mean whatever the
# attached terminal wants them to mean.

# ask the terminal for its background color (OSC 11); terminals that don't
# implement the query simply never answer, and we hit the timeout
__query_background() {
    zmodload -F zsh/system b:sysread 2>/dev/null || return 1
    local stty_state reply chunk
    stty_state=$(stty -g <$TTY 2>/dev/null) || return 1
    stty raw -echo <$TTY 2>/dev/null || return 1
    print -n '\e]11;?\e\\' >$TTY
    while sysread -t 0.3 chunk <$TTY 2>/dev/null; do
        reply+=$chunk
        [[ $reply == *($'\a'|$'\e\\')* ]] && break
    done
    stty $stty_state <$TTY 2>/dev/null
    [[ $reply == *rgb:* ]] || return 1
    print -r -- $reply
}

# the reply is rgb:RRRR/GGGG/BBBB (or one/two/three digits per channel)
__theme_from_background() {
    local r g b
    [[ $1 =~ 'rgb:([0-9a-fA-F]+)/([0-9a-fA-F]+)/([0-9a-fA-F]+)' ]] || return 1
    r=$(( 16#${match[1][1,2]} ))
    g=$(( 16#${match[2][1,2]} ))
    b=$(( 16#${match[3][1,2]} ))
    if (( (r * 299 + g * 587 + b * 114) / 1000 > 127 )); then
        print -r -- light
    else
        print -r -- dark
    fi
}

# where $THEME came from:
#   file      the marker theme(1) rewrites, which is the machine's answer
#   terminal  the terminal itself, on a machine theme(1) has never run on
#
# The marker goes first on purpose. Anything inherited - a parent shell, the
# copy tmux took of the session environment when the session was made - is a
# snapshot of some earlier theme, and believing it is how a pane ends up light
# on a black terminal.
__resolve_theme() {
    __theme_source=file
    [[ -r ~/.config/theme ]] && THEME=$(<~/.config/theme)
    [[ $THEME == (light|dark) ]] && return

    local reply
    if [[ -o interactive && -n $TTY && -z $TMUX ]] \
            && reply=$(__query_background) \
            && THEME=$(__theme_from_background $reply); then
        __theme_source=terminal
        return
    fi

    # last resort: dark text on a light background is the more readable way to
    # be wrong, since terminals that answer nothing tend to be light
    __theme_source=file
    THEME=light
}
__resolve_theme
export THEME

# everything in this shell that depends on the theme, rebuilt from scratch.
# The colors themselves come from ~/.config/theme.d, rendered by
# theme(1) out of cfg/theme/roles.toml - see cfg/theme/render.py
__apply_theme() {
    local fragment=~/.config/theme.d/$THEME.colors.zsh
    [[ -r $fragment ]] && source $fragment
    make_ps1
    [[ -n $__fzf_base_opts ]] \
        && export FZF_DEFAULT_OPTS="$__fzf_base_opts --color=$theme_fzf_colors"
    return 0
}

# theme(1) rewrites the marker file and then sends every zsh a WINCH, so
# running shells repaint without being restarted.
__refresh_theme() {
    local was=$THEME
    [[ -r ~/.config/theme ]] && THEME=$(<~/.config/theme)
    [[ $THEME == (light|dark) ]] || THEME=$was
    [[ $THEME != $was ]] && __apply_theme
    return 0
}

# `theme` says what we're on, `theme light` / `theme dark` changes it.
#
# Changing the theme means changing the machine - wallpaper, gtk, the marker
# file, every shell and every editor - so this hands over to theme(1).
theme() {
    if [[ -z $1 ]]; then
        print "$THEME (from $__theme_source)"
        return 0
    fi
    if [[ $1 != (light|dark) ]]; then
        print -u2 "usage: theme [light|dark]"
        return 1
    fi
    command theme $1 || return
    # theme(1) signals every shell, but ours arrives while it is still our
    # foreground job, so repaint here rather than wait for the next one
    THEME=$1
    __apply_theme
}

# WINCH rather than USR1 because shells that predate this config ignore it
# instead of dying, and because zsh redraws the prompt on it anyway
TRAPWINCH() {
    __refresh_theme
    zle && zle reset-prompt
    return 0
}

# the marker file is a cheap read; asking tmux costs a fork, so let the signal
# handle that case and don't do it on every single prompt
__refresh_theme_on_precmd() {
    [[ $__theme_source == file ]] && __refresh_theme
    return 0
}
precmd_functions+=(__refresh_theme_on_precmd)


# miscellaneous
unsetopt beep                           # disable beep on errors
setopt correct                          # enable [nyae] correction
setopt auto_cd                          # "cd /usr/" becomes "/usr/"
setopt nocasematch                      # regex should work case-insensitive
setopt magic_equal_subst                # enable path autocompletion after =
WORDCHARS='*?_-.[]~=&;!#$%^(){}<>'      # what kill-word should delete

# history
HISTFILE=~/.config/zsh/history  # where to put the history
HISTSIZE=50000                  # how many entries in history (memory)
SAVEHIST=$HISTSIZE              # how many entries in history (hdd)
setopt appendhistory            # append lines (for concurrent sessions)
setopt extended_history         # record usage timestamps in the histfile
setopt inc_append_history       # write to the histfile immediately
setopt share_history            # share history between all sessions
setopt hist_ignore_all_dups     # delete old entry if new entry is a duplicate
setopt hist_find_no_dups        # do not display a line previously found
setopt hist_ignore_space        # discard entries that start with space
setopt hist_save_no_dups        # don't write duplicate entries in the histfile
setopt hist_reduce_blanks       # remove extra blanks before recording entry
setopt hist_verify              # don't execute immediately upon expansion

# don't remember certain commands
# `rg` and nothing else: `rg*` also swallowed rgb, and would swallow any
# other command that happens to start with those two letters
HISTORY_IGNORE="(plen*|enpl*|jaen*|tl*|edict*|reverso*|glosbe*|sjp*|urban*|rg|rg *)"
zshaddhistory() {
  emulate -L zsh
  [[ $1 != ${~HISTORY_IGNORE} ]]
}

# basic key bindings for interactive shells
if [[ $- == *i* ]]; then
    bindkey -e                # use Emacs keybindings
    bindkey "\e" vi-cmd-mode  # ...but allow going to Vi mode
    bindkey '^X^E' edit-command-line
    export KEYTIMEOUT=1       # ...and don't block other hotkeys for too long
    stty -ixon                # reclaim ctrl+q for Neovim
    stty stop undef           # reclaim ctrl+s for Neovim
fi

# key bindings for terminal emulator
if [ "$TERM" =~ rxvt ]; then
    bindkey "\e[3~"   delete-char         # delete
    bindkey '\e[1~'   beginning-of-line   # home and ctrl+home
    bindkey '\e[4~'   end-of-line         # end and ctrl+end
    bindkey '^H'      backward-kill-word  # ctrl+backspace
    bindkey "^[3^"    kill-word           # ctrl+delete
    bindkey '\eOd'    backward-word       # ctrl+left
    bindkey '\eOc'    forward-word        # ctrl+right

    bindkey '\e[1;5I' 'beep'              # disable ctrl+tab
    bindkey '\e[1;6I' 'beep'              # disable ctrl+shift+tab
else
    bindkey "\e[3~"   delete-char         # delete
    bindkey '\eOH'    beginning-of-line   # home
    bindkey '\eOF'    end-of-line         # end
    bindkey ';5H'     beginning-of-line   # ctrl+home
    bindkey ';5F'     end-of-line         # ctrl+end
    bindkey '^_'      backward-kill-word  # ctrl+backspace
    bindkey "\e[3;5~" kill-word           # ctrl+delete
    bindkey ';5D'     backward-word       # ctrl+left
    bindkey ';5C'     forward-word        # ctrl+right

    bindkey '\e[1;5I' 'beep'              # disable ctrl+tab
    bindkey '\e[1;6I' 'beep'              # disable ctrl+shift+tab
fi

# command prompt
__git_ps1 () {
    local g="$(command git rev-parse --git-dir 2>/dev/null)"
    if [ -n "$g" ]; then
        local r
        local b
        if [ -d "$g/rebase-apply" ]; then
            if test -f "$g/rebase-apply/rebasing"
            then
                r="|REBASE"
            elif test -f "$g/rebase-apply/applying"
            then
                r="|AM"
            else
                r="|AM/REBASE"
            fi
            b="$(command git symbolic-ref HEAD 2>/dev/null)"
        elif [ -f "$g/rebase-merge/interactive" ]; then
            r="|REBASE-i"
            b="$(cat "$g/rebase-merge/head-name")"
        elif [ -d "$g/rebase-merge" ]; then
            r="|REBASE-m"
            b="$(cat "$g/rebase-merge/head-name")"
        elif [ -f "$g/MERGE_HEAD" ]; then
            r="|MERGING"
            b="$(command git symbolic-ref HEAD 2>/dev/null)"
        else
            if [ -f "$g/BISECT_LOG" ]; then
                r="|BISECTING"
            fi
            if ! b="$(command git symbolic-ref HEAD 2>/dev/null)"; then
                if ! b="$(command git describe --exact-match HEAD 2>/dev/null)"; then
                    b="$(cut -c1-7 "$g/HEAD")..."
                fi
            fi
        fi

        if [ -n "${1-}" ]; then
            printf "$1" "${b##refs/heads/}$r"
        else
            printf "(%s)" "${b##refs/heads/}$r"
        fi
    fi
}

function make_ps1 () {
    # the colors come from ~/.config/theme.d/$THEME.colors.zsh, which
    # __apply_theme sourced just before calling us. color1 and color2 stay
    # global on purpose: PS1 expands them at prompt time
    local host=$(hostname)
    color1=${theme_ps1_host[$host]:-$theme_ps1_host[default]}
    color2=${theme_ps1_host2[$host]}

    # user@host
    PS1='${color1}%n@${color2}%m%b'

    # colon
    PS1+="$theme_ps1_punct:"

    # path relative to $HOME
    PS1+="$theme_ps1_path%~"

    # git prompt
    setopt prompt_subst
    PS1+="$theme_ps1_git"'`__git_ps1`'

    # colored $ depending on last exit code
    PS1+="%(?.$theme_ps1_ok.$theme_ps1_err)\$%f%k "

    # reset colors
    PS1+="%f"

    # add titlebar user@host:~/
    if [[ "$TERM" != linux ]]; then
        PS1+=$'%{\e]0;%n@%M:%~\007%}'
    fi
}
# config fzf
if [ -f ~/.fzf.zsh ]; then
    export FZF_DEFAULT_COMMAND='rg --files --hidden'
    # kept apart from --color so that __apply_theme can rebuild the option
    __fzf_base_opts="$FZF_DEFAULT_OPTS --inline-info"
    export FZF_DEFAULT_OPTS="$__fzf_base_opts --color=$theme_fzf_colors"
    source ~/.fzf.zsh
    bindkey '\C-o' fzf-file-widget  # choose c-o for file opener
    bindkey '\C-t' transpose-chars  # restore default binding for c-t
fi

# don't be a nag when working with gui via ssh
if [ ! -v DISPLAY ]; then
    export DISPLAY=:0.0
fi

# set env variables for certain commands
export HOST_USER_UID=$(id -u)
export HOST_USER_GID=$(id -g)

# change cursor for vim insert mode
zle-line-init zle-keymap-select () {
    case $KEYMAP in
        vicmd)
            if [[ $TMUX = '' ]]; then
                echo -ne "\033]12;red\007"
            else
                printf '\033Ptmux;\033\033]12;red\007\033\\'
            fi
            ;;
        viins|main)
            if [[ $TMUX = '' ]]; then
                echo -ne "\033]112\007"
            else
                printf '\033Ptmux;\033\033]112\007\033\\'
            fi
            ;;
    esac
}
zle -N zle-line-init
zle -N zle-keymap-select
zle -N edit-command-line

# pyenv
export PYENV_ROOT="$HOME/.pyenv"
if [[ -d $PYENV_ROOT/bin ]]; then
    export PATH="$PYENV_ROOT/bin:$PATH"
    eval "$(pyenv init -)"
fi

# paint the prompt and friends now that everything they need is defined
__apply_theme
